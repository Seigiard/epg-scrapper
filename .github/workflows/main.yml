name: Cron Action
on:
  schedule:
    - cron: '* */4 * * *'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm i
          node index.js docs/epg.xml
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs/epg.xml
          git commit -m "generated epg"
          git push
